---
description: 使用 L3 深度白名单渗透策略
argument-hint: "<白名单、强度限制、时间窗口和目标说明>"
---

# /deep-l3

用途：使用 L3 深度白名单策略，在明确白名单和强度限制后进行受控深度测试、接口猜测、参数猜测和小范围 fuzz。

执行规则：

1. 先读取 `CLAUDE.md`、当前项目声明和 `Competence/workflow-and-verification.md`。
2. 启用前必须明确白名单资产、路径、接口、模块或业务流程。
3. 启用前必须明确字典规模、请求速度、并发限制、时间窗口和熔断条件。
4. 在白名单内做受控接口猜测、参数猜测、枚举值猜测、回头看项复测或小范围 fuzz。
5. 如果白名单内存在 Web JS、前端源码、小程序解包代码、App 源码或 App 反编译代码，必须全量审计，并结合接口猜测、参数猜测和漏洞假设复核。
6. 明显低概率、低价值的位置不测；连续低收益时收缩方向。
7. 不跨白名单，不使用破坏性 payload；危险接口禁止测试，除非项目声明或补充说明明确允许具体接口和方式。
8. 未覆盖范围必须标记为“未完成”或“未测试”。

输出：

- 策略层级：L3
- 白名单：
- 强度限制：
- 熔断条件：
- 批次结果：
- 未完成范围：
