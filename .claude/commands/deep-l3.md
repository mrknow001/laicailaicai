---
description: 预留 L3 深度白名单渗透策略入口
argument-hint: "<目标或策略讨论说明>"
---

# /deep-l3

用途：预留 L3 深度白名单渗透策略入口。

当前状态：策略灵魂尚未精细化落地。

执行规则：

1. 不要自行扩写 L3 的具体 fuzz 或深度测试动作。
2. 不要因为用户调用本命令就自动进行目录 fuzz、接口 fuzz 或大字典测试。
3. 只能读取项目声明，整理 L3 所需的白名单、并发上限、字典规模、时间窗口和熔断条件。
4. 可以帮助用户讨论 L3 策略应如何受控落地。
5. 如需实际测试，必须等 L3 策略正式落地后再执行。

预留定位：

- 深度白名单
- 受限 fuzz
- 明确并发
- 明确字典规模
- 明确熔断条件
- 禁止破坏性 payload
