# 漏洞假设

> 漏洞假设用于把线索转成可验证方向。先判断价值和边界，再考虑验证。

## 假设清单

| 编号 | 线索来源 | 漏洞假设 | 目标 / 接口 | 价值判断 | 风险边界 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| H-001 |  |  |  | 高 / 中 / 低 | 低风险 / 需确认 / 禁止 | 最小化验证思路 | 待验证 / 回头看 / 已验证 / 已排除 / 暂停 |

## 回头看队列

> 记录“一线之差”的候选漏洞。当前缺少参数、ID、枚举值、状态值、token 或上下文时，不要无止境猜测；后续遇到关键值再回头复测。渗透结束后用户也会查看这里，所以必须写清楚为什么值得回头看，以及用户可以补充什么信息。

| 编号 | 关联假设 | 接口 / 接口组 | 为什么值得回头看 | 当前缺口 | 需要用户补充 | 回头触发条件 | 后续复测结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | H-001 |  |  |  |  | 遇到相关参数 / ID / 枚举值 / 状态值 / token / 上下文 |  |

## 优先关注方向

- 未授权访问：
- 越权访问：
- 敏感信息泄露：
- 认证绕过：
- 严重逻辑漏洞：
- SQL 注入存在性证明：
- 文件上传 / 下载风险：
- RCE / 命令执行存在性证明：

## 暂不关注方向

- 普通 URL 跳转：
- 普通反射 XSS：
- 低价值配置暴露：
- 其他低收录价值问题：
