# 作战计划 / 任务编排

> 复杂或多目标渗透的任务拆解与进度，是**跨会话可移植的权威进度源**。
> 开工前先读它续进度；每完成一项、或被打断 / 切目标前，先更新本文件再继续——
> 这样即使上下文被压缩、精度下降，也能从这里恢复「做到哪、还剩什么、当前在做哪一项」。
> 会话内执行用任务列表逐项跟踪；简单任务可直接做，不必拆到这里。

## 完成度

**0 / 0** 完成 ｜ 更新于：

## 状态约定

`[ ]` 待办 · `[~]` 进行中（同时只允许一个）· `[部分]` 部分测试 · `[x]` 完成 · `[!]` 未完成（阻塞 / 缺信息）

## 当前批次

| # | 任务 | 范围 / 目标 | 完成标准（最小完整验证闭环） | 状态 | 关联 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  | `[ ]` | hypotheses / tests |

## 未进入本批（分批留存）

- 

## 阻塞 / 需用户确认

- 
