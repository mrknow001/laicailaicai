# 项目模板说明

这是标准项目模板。创建新项目时，复制整个 `_template` 目录到 `projects/<项目标识>/`，再按项目声明填写内容。

建议创建流程：

1. 复制 `_template`。
2. 填写 `project.md`。
3. 同步更新 `scope.md` 的白名单和黑名单摘要。
4. 后续所有线索、接口、测试、漏洞、证据和报告都写入该项目目录。

不要每次临时新建一套文件，避免字段、命名和记录风格不统一。
